---
name: hwpx-style-transplant
description: >
  HWPX 서식 이식. 파일 A의 글꼴·크기·색·줄간격·선굵기·여백만 가져오고,
  파일 B의 내용만 가져와 C를 만든다. 한글 양식, hwpx, 서식 복사, 내용 이식
  요청이면 이 스킬과 references/ schemas/ examples/를 반드시 사용한다.
---

사용자는 보통 파일 두 개를 올린다.

- **파일 A = 서식 원본(스타일 템플릿).** 글꼴, 크기, 색, 줄간격, 선 굵기, 여백, 정렬, 표 테두리 등 보기만 가져온다.
- **파일 B = 내용 원본.** 문단, 제목, 표, 목록, 이미지, 각주 등 글과 구조만 가져온다.

파일이 두 개인데 어느 쪽이 A/B인지 안 적혀 있으면, 먼저 짧게 묻는다.

> "어느 파일이 서식(A)이고, 어느 파일이 내용(B)인가요?"

사용자가 "첫 번째가 양식, 두 번째가 내용"처럼 답하면 그대로 진행한다.

파일이 하나만 있으면 진행하지 말고, 서식 파일 A와 내용 파일 B를 모두 올려 달라고 요청한다. `.hwp`(구 바이너리)가 오면 HWPX로 다시 저장해 올려 달라고 안내한다. 억지로 변환을 가정하지 않는다. B에도 서식이 있지만, C에서는 모두 버린다.

1. A/B 확정, 파일 상태 확인
   - A가 서식 원본, B가 내용 원본인지 확정한다. 모르면 한 번만 묻는다.
   - 파일 하나만 있으면 진행하지 말고, 서식 파일 A와 내용 파일 B를 모두 올려 달라고 요청한다.
   - `.hwp`(구 바이너리)가 오면 HWPX로 다시 저장해 올려 달라고 안내한다. 억지로 변환을 가정하지 않는다.
   - 파일이 열리지 않거나 내용을 못 읽으면, 그 사실을 알리고 중단한다. 추측으로 문서를 만들지 않는다.

2. `references/00-solar-pro-4-protocol.md`를 따른다. 중간 판단을 기억에 의존하지 말고, 숫자·서식은 파일/스키마 기준으로 채운다.

3. A를 읽고 **서식 프로필**만 채운다. **A의 문장은 추출하지 않는다.**
   - 글자 서식: 글꼴 이름, 크기(pt), 글자색·강조색·형광펜/배경색, 굵게·기울임·밑줄·취소선, 자간, 장평
   - 문단 서식: 줄간격, 문단 위·아래 간격, 왼쪽/오른쪽 여백, 첫 줄 들여쓰기, 정렬, 문단 테두리·선 굵기·색
   - 페이지 서식: 용지 크기, 가로/세로, 위·아래·왼쪽·오른쪽 여백, 머리글/바닥글 서식(내용은 안 가져옴)
   - 표·상자 서식: 테두리 선 굵기·종류·색, 셀 배경색, 헤더 배경색, 셀 안 여백, 셀 글꼴·크기·색
   - 역할별 스타일: 문서 제목 / 제목 1·2·3 / 본문 / 캡션·주석 / 목록 / 표 헤더·표 본문 / 인용
   - 읽지 못한 값은 빈칸으로 두지 말고 `unknown` + 이유를 적는다.
   - 이 값을 `schemas/style-profile.schema.json`에 채운다.

4. B를 읽고 **내용 골격**만 채운다. **B의 서식은 버린다.**
   - 문단 순서 그대로의 텍스트
   - 제목인지 본문인지의 구조 판단
   - 목록(글머리·번호와 항목 텍스트)
   - 표(행×열 구조, 셀 텍스트, 병합 여부)
   - 이미지(있으면 위치와 설명, 옮길 수 있으면 옮김)
   - 각주·미주·캡션 텍스트
   - 페이지 나누기가 내용상 의미 있으면 유지하되, 단순 서식용 빈 줄은 과도하게 복제하지 않는다.
   - 이 값을 `schemas/content-skeleton.schema.json`에 채운다.

5. `references/06-mapping-and-id-remap.md`로 블록별 역할을 붙인다.
   - B의 “문서 제목” → A의 문서 제목 스타일
   - B의 제목 1·2·3 → A의 제목 1·2·3. A에 제목 3이 없으면 제목 2, 그것도 없으면 본문+굵게
   - B의 본문 → A의 본문
   - B의 표 → A의 표 테두리·셀 색·셀 글꼴을 적용. 셀 안의 글과 행·열 수는 B 그대로
   - B의 목록 → A의 목록 글꼴·간격. 글머리 기호는 A에 표준 기호가 있으면 A를 따르고, 없으면 B의 기호 문자는 유지
   - 이미지는 B에서 가져오되, 테두리·캡션 서식은 A를 따름
   - C의 페이지 여백·용지·기본 글꼴·줄간격·선 굵기·표 서식은 전부 A이다.
   - A의 `charPr`·`paraPr`·`style`·`borderFill` ID를 B 블록에 그대로 붙이지 않는다. ID를 섞지 않는다.

6. C를 만든다. `schemas/transplant-report.schema.json`을 채운 뒤, 아래 순서로 낸다.
   - ① 이식 요약표
   - ② 주의/누락
   - ③ 결과 파일 C
   - 중간 JSON/스키마 내용은 사용자에게 길게 보여 주지 않는다. 사용자에게는 요약표·누락·C만 보인다.

7. `checklists/qa-before-delivery.md`를 통과하기 전에는 파일을 주지 않는다.
   - C의 본문 문장이 B와 같은지, A의 문장이 섞이지 않았는지
   - C의 글꼴·크기·색·줄간격·여백이 A를 따르는지
   - B의 표 셀 값과 행·열 수가 유지됐는지
   - 내용을 요약하거나 예쁘게 고쳐 쓰지 않았는지
   - 추정 값을 추정이라고 밝혔는지

## 지식 파일 — 필요할 때 연다

| 상황 | 연 파일 |
|------|---------|
| 작업 시작, 모델 실수 방지 | `references/00-solar-pro-4-protocol.md` |
| ZIP/XML이 보이거나 파일을 뜯어야 할 때 | `references/01-hwpx-package.md` |
| 숫자 단위 (pt, mm, %, HWPUNIT) | `references/02-units.md` |
| A에서 무엇을 뽑을지 | `references/03-style-catalog.md` |
| B에서 무엇을 뽑을지 | `references/04-content-catalog.md` |
| 제목/본문/표 역할을 헷갈릴 때 | `references/05-role-classification.md` |
| A 서식을 B 블록에 붙일 때 | `references/06-mapping-and-id-remap.md` |
| 표·선굵기·이미지 | `references/07-tables-borders-images.md` |
| 사용자에게 보이는 답 형식 | `references/08-output-contract.md` |
| 자주 하는 실수 | `references/09-failure-modes.md` |
| 비슷한 사례가 필요할 때 | `examples/` 전부 |
| 제출 직전 | `checklists/` |

## 절대 규칙

1. A의 텍스트·표 값·날짜·이름을 C에 넣지 않는다.
2. B의 글꼴·색·줄간격·여백·선굵기를 C에 넣지 않는다.
3. B의 문장을 윤문·요약·번역하지 않는다.
4. A에 없는 스타일을 창작하지 않는다. 없으면 본문으로 떨어뜨린다.
5. `charPr` / `paraPr` / `style` / `borderFill` **ID를 A↔B 사이에 그대로 복사하지 않는다.** 숫자 ID는 파일마다 다른 객체다.
6. 읽지 못한 값은 빈칸으로 두지 말고 `unknown` + 이유를 적는다. 지어내지 않는다.
7. `.hwp`(구 바이너리)는 HWPX로 다시 저장해 달라고 하고 중단한다.

## 도구

- 파일 업로드, 문서 읽기, 문서 생성, 추론(reasoning)을 켠다.
- HWPX를 ZIP으로 열어 `Contents/header.xml`과 `Contents/section*.xml`을 읽을 수 있으면 그렇게 한다.
- 못 열면 렌더된 텍스트·미리보기로 프로필을 채우되 모든 숫자에 `source: "visual-estimate"`를 붙인다.
- HWPX를 저장할 수 없으면 DOCX/PDF로 떨어뜨리고 `references/08-output-contract.md`의 폴백을 따른다.
